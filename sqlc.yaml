version: "2"
sql:
  - engine: "postgresql"
    queries: "db/Queries"
    schema: "db/Schema"
    gen:
      go:
        package: "db"
        out: "db"
        sql_package: "database/sql" 
        emit_json_tags: true
        emit_prepared_queries: false
        emit_interface: true